# PickMyAI TrapNet — 법적 안전 가이드라인

> 허니팟·기만 방어(Deceptive Defense) 운영 시 법적 리스크를 제거하기 위한 정책 문서.
> 마지막 업데이트: 2026-04-11

---

## 1. "함정 수사(Entrapment)" vs "방어적 기만(Defensive Deception)"

### 핵심 구분

| 구분 | 함정 수사 (불법) | 방어적 기만 (합법) |
|------|-----------------|-------------------|
| 주체 | 수사기관이 범죄를 유발 | 시스템 소유자가 자산을 보호 |
| 행위 | 범죄 의사가 없는 자를 유인 | 이미 침입 의사를 가진 자가 스스로 접근 |
| 목적 | 범죄 증거 확보 | 공격 탐지 및 차단 |
| 우리의 경우 | **해당 없음** | **해당** — 정상 사용자는 절대 접근하지 않는 경로만 감시 |

### 왜 합법인가

1. **자발적 접근 원칙**: `/api/graphql`, `/.env`, `/wp-admin` 등은 정상 사용자가 접근할 이유가 없음. 이 경로에 대한 요청은 공격자의 자발적 스캐닝 행위.
2. **유인 행위 없음**: 허니팟 경로를 외부에 광고하거나, 링크를 노출하거나, 접근을 유도하지 않음. robots.txt에도 미포함.
3. **비례 원칙 준수**: 응답은 IP 차단 + 가짜 데이터 반환. 역해킹, 공격, 데이터 탈취 등 공격적 행위를 수행하지 않음.
4. **민간 방어 권리**: 한국 정보통신망법 제48조는 "정당한 접근 권한 없이" 시스템에 침입하는 행위를 금지. 허니팟은 이러한 불법 접근을 탐지하는 방어적 수단.

---

## 2. 가짜 데이터(Canary Token) 법적 안전 요건

### 우리의 가짜 데이터가 충족하는 요건

| 요건 | 충족 여부 | 설명 |
|------|----------|------|
| **실제 시크릿 미포함** | O | 가짜 AWS 키는 `AKIATRAP...` 접두사, 실제 AWS 계정과 무관 |
| **PII 미포함** | O | 가짜 데이터에 실제 사용자 이름, 이메일, 전화번호 없음 |
| **역추적 자체가 목적 아님** | O | Canary marker(`_canary_marker: pickmyai-honeypot-token`)는 내부 식별용 |
| **제3자 시스템 손상 없음** | O | 가짜 DB IP(`10.0.x.x`)는 RFC1918 사설 대역, 외부 서버에 영향 없음 |
| **명시적 식별 가능** | O | 소스코드와 응답에 `canary`, `trap`, `honeypot` 마커 포함 |

### 금지 사항

- **절대** 실제 작동하는 AWS/GCP/Azure 크리덴셜을 허니팟에 사용하지 않음
- **절대** 가짜 데이터로 공격자의 시스템에 접근(reverse hacking)하지 않음
- **절대** 가짜 데이터에 실제 사용자 PII를 삽입하지 않음
- **절대** 허니팟 경로를 외부에서 검색 가능하게 SEO 노출하지 않음

---

## 3. 로그 기록 정책 — 개인정보보호법/GDPR 준수

### 3.1 수집하는 데이터

| 데이터 항목 | 수집 여부 | 근거 |
|------------|----------|------|
| IP 주소 | O | 정당한 이익(Legitimate Interest) — 공격 차단 필수 |
| User-Agent | O | 공격 도구 식별 필수 |
| 접근 경로 | O | 허니팟 히트 분석 |
| 타임스탬프 | O | 시계열 분석 |
| 요청 헤더 (선별) | O | 공격 fingerprint |
| 쿠키 내용 | X | 수집하지 않음 |
| 요청 Body | X | 수집하지 않음 |
| 사용자 계정 정보 | X | 허니팟 로그에 userId 미포함 |

### 3.2 IP 주소의 개인정보 해당 여부

- EU GDPR: IP 주소는 개인정보로 간주 → 수집 근거 필요
- 한국 개인정보보호법: 다른 정보와 결합하여 개인 식별 가능 시 개인정보
- **우리의 근거**: GDPR Art. 6(1)(f) "정당한 이익" — 사이버 보안 목적의 IP 수집은 Recital 49에서 명시적으로 허용

### 3.3 보존 기간 및 삭제 정책

| 로그 유형 | 보존 기간 | 삭제 방법 |
|----------|----------|----------|
| 인메모리 차단 목록 (`honeypotBanMap`) | 24시간 | 자동 만료 |
| 인메모리 트랩 히트 카운터 | 10분 | 자동 만료 |
| Discord/Slack 알림 | 90일 | 채널 보존 정책 따름 |
| Supabase `audit_logs` | 180일 | `pg_cron` 자동 삭제 예약 |
| Rate limit 카운터 | 1~15분 | 자동 만료 |

### 3.4 로그에서 제외해야 하는 정보 (sanitization)

```
절대 로그에 포함하지 않는 것:
- 사용자 비밀번호 (평문/해시 모두)
- JWT 토큰 전체 (앞 10자까지만 마스킹 허용)
- API 키 전체
- 쿠키 값
- 요청 Body 내용
- 사용자 이메일 (허니팟 로그에서)
```

이 정책은 `src/lib/security.ts`의 `sanitizeResponse` 함수에 구현되어 있음.

---

## 4. 국가별 법적 참조

### 한국

- **정보통신망법 제48조**: 정당한 접근 권한 없이 정보통신망에 침입하는 것은 불법. 허니팟은 이를 탐지하는 합법적 방어.
- **개인정보보호법 제15조**: 정보주체의 동의 없이 개인정보 수집 가능한 경우 — "정보통신서비스 제공자의 정당한 이익을 위하여 필요한 경우" (제6호).
- **전자상거래법**: 결제 관련 로그는 5년 보존 의무 (세무 목적).

### EU/GDPR

- **Art. 6(1)(f)**: Legitimate Interest — 네트워크 보안 목적 데이터 처리 허용.
- **Recital 49**: "processing of personal data to the extent strictly necessary and proportionate for the purposes of ensuring network and information security" — **명시적 허용**.
- **Art. 5(1)(e)**: Storage Limitation — 목적 달성 후 삭제 의무 → 위 보존 기간 준수.

### 미국

- **CFAA (Computer Fraud and Abuse Act)**: 방어적 허니팟은 "unauthorized access"에 해당하지 않음. 자기 시스템에 함정을 설치하는 것은 합법.
- **각 주 데이터 보호법 (CCPA 등)**: 보안 목적 로그 수집은 예외 허용.

---

## 5. 개인정보 영향 평가(PIA) 요약

| 항목 | 평가 |
|------|------|
| 수집 목적 적합성 | 적합 — 사이버 공격 탐지/차단 |
| 수집 최소화 | 준수 — IP, UA, 경로, 시간만 수집 |
| 비례 원칙 | 준수 — 정상 사용자 데이터 미수집, 허니팟 경로 접근자만 대상 |
| 안전성 확보 | 준수 — 인메모리 + 자동 만료, 영속 로그는 Supabase RLS 보호 |
| 정보주체 권리 | 해당 없음 — 공격자는 정보주체 권리 행사 대상이 아님 (GDPR Art. 23 예외) |
| 국외 이전 | Discord/Slack 웹훅은 미국 서버 — 알림에 IP만 포함, 최소 정보 |

---

## 6. 운영 체크리스트

- [ ] `SECURITY_WHITELIST_IPS` 환경변수에 내부 모니터링 서버 IP 등록
- [ ] robots.txt에 허니팟 경로를 절대 포함하지 않음 (검색엔진 크롤러 유인 방지)
- [ ] Discord/Slack 채널에 90일 자동 삭제 정책 적용
- [ ] Supabase `audit_logs`에 180일 자동 삭제 pg_cron 작업 등록
- [ ] 연 1회 이상 허니팟 오탐(false positive) 로그 리뷰
- [ ] 가짜 크리덴셜의 `AKIATRAP` 접두사가 실제 AWS 계정과 무관함을 확인
- [ ] 개인정보처리방침에 "보안 목적 로그 수집" 조항 포함 확인

---

## 7. 전자상거래법 / 통신판매업 / 개인정보보호법 준수 사항

### 7.1 전자상거래법 제6조 — 거래 기록 보존 의무

| 기록 유형 | 법정 보존 기간 | 구현 상태 |
|----------|-------------|----------|
| 표시/광고 기록 | 6개월 | 해당 없음 (광고 시스템 미사용) |
| 계약/청약철회 기록 | 5년 | `transactions` 테이블 — `retention_expires_at` 트리거로 5년 자동 설정 |
| 대금결제/재화공급 기록 | 5년 | 동일 |
| 소비자 불만/분쟁 처리 | 3년 | `audit_logs` — 6개월 보존 (분쟁 발생 시 별도 보관 필요) |

**핵심 변경**: `transactions.user_id`의 `ON DELETE CASCADE`를 `ON DELETE SET NULL`로 변경.
사용자 탈퇴 시 개인 식별 연결은 해제되지만 거래 기록 자체는 5년간 보존됨.

### 7.2 통신판매업 신고 의무사항 (전자상거래법 제13조)

웹사이트에 반드시 표시해야 하는 정보:

```
필수 표시 항목 (환경변수로 관리):
NEXT_PUBLIC_BIZ_NAME            — 상호
NEXT_PUBLIC_BIZ_REPRESENTATIVE  — 대표자 성명
NEXT_PUBLIC_BIZ_REGISTRATION_NO — 사업자등록번호
NEXT_PUBLIC_BIZ_SALES_NO        — 통신판매업 신고번호
NEXT_PUBLIC_BIZ_ADDRESS         — 영업소 소재지
NEXT_PUBLIC_BIZ_EMAIL           — 이메일
```

### 7.3 개인정보보호법 — 회원 탈퇴 시 처리

- `POST /api/auth/withdraw` — 사용자 자가 탈퇴 API
- `withdraw_user` RPC — 거래 기록 비식별화(user_id → NULL) + 개인정보 즉시 삭제
- 탈퇴 확인 시 `confirm: "탈퇴합니다"` 문자열 필수 (오조작 방지)

### 7.4 정보통신망법 시행령 제15조 — 접속 기록 보관

| 로그 유형 | 법정 최소 보관 | 실제 보관 |
|----------|-------------|----------|
| 접속 기록 (로그인/로그아웃) | 6개월 | `audit_logs` — 180일 (pg_cron) |
| 접속 기록 (개인정보 처리) | 2년 (5만 명 이상 정보주체) | 추후 규모 확대 시 조정 필요 |

### 7.5 운영 체크리스트 (추가)

- [ ] `.env`에 사업자 정보 환경변수 6개 설정
- [ ] Supabase에 `supabase-compliance-migration.sql` 실행
- [ ] 통신판매업 신고 완료 후 신고번호 환경변수 업데이트
- [ ] 설정 페이지에 회원탈퇴 UI 버튼 추가
- [ ] 환불 정책 전용 페이지 작성 (현재 FAQ에만 존재)
